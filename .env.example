# Variables de entorno para CharlaMente

# Entorno de ejecución (development o production)
DJANGO_ENV=development

# Configuración de Django
SECRET_KEY=your_secret_key_here
DEBUG=True

# Hosts permitidos (separados por comas)
ALLOWED_HOSTS=127.0.0.1,localhost

# Configuración de MySQL
DB_ENGINE=django.db.backends.mysql
DB_NAME=charlamente_db
DB_USER=root
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=3306

# Configuración de correo (para futura implementación)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your_email@gmail.com
# EMAIL_HOST_PASSWORD=your_app_password

# APIs (para futura implementación)
# OpenAI o alternativa para resúmenes
# LLM_API_KEY=your_llm_api_key
# LLM_MODEL=gpt-3.5-turbo

# API de transcripción (para futura implementación)
# TRANSCRIPTION_API_KEY=your_transcription_api_key
# TRANSCRIPTION_MODEL=whisper-1